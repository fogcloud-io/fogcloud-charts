{{ if eq .Values.rabbitmq.type "internal" }}
{{ $persistence := .Values.rabbitmq.internal.persistence }}
{{ if not $persistence.existed }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    "helm.sh/resource-policy": keep
  labels:
    pvc: rabbitmq-data
  name: {{ $persistence.pvc }}
  
spec:
  storageClassName: {{ $persistence.storageClassName }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: "{{ $persistence.storage }}"
status: {}
{{ end }}
{{ end }}